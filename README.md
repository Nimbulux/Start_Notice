# Start_Notice
> [!IMPORTANT]
> 本程序没有UI 需要自行创建`config.txt`并向其中写入需要启动的应用程序

一个小小的工具 因为学校的老师都不太会用电脑 再加上有的时候电脑很慢 所以就会点好多次 这样越开越多 甚至白板会出问题出一个透明的覆盖到屏幕上 然后老师就彻底不会了 就会归罪在我的头上 于是便写了这个小程序

## 关于config.txt
内容为一行目标程序路径 如`my_app.exe`
文件保存为 `UTF‑8` 即可
与 `Start_Notice.exe` 放在同一目录
> [!NOTE]
> 目标程序也应该在此目录下
